# Include host git config for user identity
[include]
	path = /root/.gitconfig.host

[core]
	editor = vi

# Prevent pushing to any remote
[remote "origin"]
	pushurl = no_push

# Alias push to prevent accidents
[alias]
	push = "!echo 'Push is disabled in Docker container. Commits are local only.' && false"
