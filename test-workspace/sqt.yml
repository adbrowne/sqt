name: test_workspace
version: 1

model_paths:
  - models

targets:
  dev:
    type: duckdb
    database: target/dev.duckdb
    schema: main

default_materialization: view

models:
  raw_events:
    materialization: table
  user_stats:
    materialization: table
